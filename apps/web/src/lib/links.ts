/**
 * Shared external links for the open-source calls-to-action.
 *
 * agentlint is a free, MIT-licensed project. The only "support" surfaces are a
 * GitHub star and an optional tip (the latter via the VietQR shown in the
 * footer) — and they are intentionally honest: nothing here is required to use
 * the tool.
 */

/** Source repository — the "Star on GitHub" CTA target. */
export const GITHUB_URL = 'https://github.com/bacnguyenne/agentlint';
