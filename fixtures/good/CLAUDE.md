# Project guidance

This project uses TypeScript with strict mode. Run `npm test` before committing.
Reference secrets via `${ENV_VAR}` placeholders such as `${API_TOKEN}`; never
paste literal keys here.
