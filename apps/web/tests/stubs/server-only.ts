// Test stub for the Next.js `server-only` package, which has no runtime body
// (it only exists to make the bundler error if imported into a client bundle).
// In unit tests there is no bundler boundary, so an empty module is correct.
export {};
